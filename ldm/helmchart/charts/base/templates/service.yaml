apiVersion: v1
kind: Service
metadata:
  name: ldm-base
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ldmbase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ldmbase.selectorLabels" . | nindent 4 }}
